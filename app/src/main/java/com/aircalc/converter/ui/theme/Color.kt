package com.aircalc.converter.ui.theme

import androidx.compose.ui.graphics.Color

// Light theme colors
val PrimaryRed = Color(0xFFA31B1B)      // Primary Red
val PrimaryGreen = Color(0xFF014B38)    // Primary Green
val CreamBackground = Color(0xFFFFF8E6) // Cream Background
val PureWhite = Color(0xFFFFFFFF)       // Pure White
val PureBlack = Color(0xFF000000)       // Pure Black
val LightGray = Color(0xFFF0F0F0)       // Light Gray
val MediumGray = Color(0xFFC9C9C9)      // Medium Gray

// Dark theme colors
val DarkPrimaryRed = Color(0xFFA94442)      // Dark red for buttons
val DarkPrimaryGreen = Color(0xFF1B5E51)    // Dark teal/green for primary button
val DarkBackground = Color(0xFF000000)      // Pure black background
val DarkSurface = Color(0xFF3A3A3C)         // Dark gray surface for cards
val DarkSurfaceVariant = Color(0xFF48484A)  // Slightly lighter gray variant
val DarkOnBackground = Color(0xFFF0F2F5)    // Off-white text on dark background
val DarkGray = Color(0xFF636366)            // Medium dark gray
val DarkInputBackground = Color(0xFF252525) // Dark neutral gray for inputs
val DarkInputText = Color(0xFFF0F2F5)       // Off-white text for inputs

// Derived colors for Material3 theme
val PrimaryRed80 = Color(0xFFE8A5A5)    // Light red for containers
val PrimaryRed40 = PrimaryRed           // Primary red
val PrimaryGreen80 = Color(0xFFA5C9B8)  // Light green for containers
val PrimaryGreen40 = PrimaryGreen       // Primary green

// Legacy colors (keeping for compatibility)
val WarmOrange80 = CreamBackground
val Terracotta80 = LightGray
val Sand80 = LightGray

val WarmOrange40 = PrimaryRed
val Terracotta40 = PrimaryRed
val Sand40 = PrimaryGreen

val WarmWhite = PureWhite
val LightCream = CreamBackground
val SoftBeige = MediumGray
val WarmGray = MediumGray
val DeepTerracotta = PrimaryRed
val DarkBrown = PureBlack