# Rename this file to gradle.properties and fill in your values
# DO NOT commit gradle.properties to version control!

# Release signing configuration
RELEASE_STORE_FILE=release-keystore.jks
RELEASE_STORE_PASSWORD=your_store_password_here
RELEASE_KEY_ALIAS=aircalc
RELEASE_KEY_PASSWORD=your_key_password_here

# To create a keystore, run:
# keytool -genkey -v -keystore release-keystore.jks -keyalg RSA -keysize 2048 -validity 10000 -alias aircalc
